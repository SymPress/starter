name: CodeQL

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: '25 3 * * 1'

permissions:
  contents: read
  security-events: write

jobs:
  analyze:
    uses: sympress/workflows/.github/workflows/codeql.yml@v1
    with:
      languages: php
