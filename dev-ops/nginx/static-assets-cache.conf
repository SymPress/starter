# Browser cache policy for versioned WordPress assets and generated media.
# Include this inside the server block, after WordPress public-path rewrites
# and before the catch-all/PHP locations.
location ~* \.(?:avif|css|eot|gif|ico|jpe?g|js|json|mjs|mp4|ogg|otf|png|svg|ttf|webm|webp|woff2?)$ {
    try_files $uri =404;
    access_log off;
    log_not_found off;

    add_header Cache-Control "public, max-age=31536000, immutable" always;
}
